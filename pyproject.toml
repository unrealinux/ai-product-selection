[project]
name = "ai-product-selection"
version = "0.1.0"
description = "AI 选品库：规则引擎 + 大模型的多维度选品打分与排序"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
